{
  "default_menu_id": {
    "description": "默认菜单方案 ID",
    "type": "string",
    "default": "default",
    "hint": "发送 /menu 或 /菜单 且不带参数时使用的菜单方案。"
  },
  "render_width": {
    "description": "默认渲染宽度",
    "type": "int",
    "default": 900,
    "hint": "图片 HTML 模板的默认宽度，实际范围会限制在 520-1400。"
  },
  "render_mode": {
    "description": "菜单图片渲染模式",
    "type": "string",
    "default": "browser",
    "options": [
      "browser",
      "auto",
      "remote",
      "pillow"
    ],
    "hint": "browser 为调用系统 Edge/Chrome 无头截图（与 Page 实时预览同款排版，推荐）；auto 先尝试 browser，失败后再尝试 AstrBot 远程 T2I；remote 仅使用远程 T2I；pillow 降级为基础绘图引擎。"
  },
  "show_render_error_detail": {
    "description": "聊天侧显示详细渲染错误",
    "type": "bool",
    "default": false,
    "hint": "建议仅调试时启用，避免向普通聊天暴露内部错误。"
  }
}

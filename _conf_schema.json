{
  "default_menu_id": {
    "description": "默认菜单方案 ID",
    "type": "string",
    "default": "default",
    "hint": "发送 /menu 或 /菜单 且不带参数时使用的菜单方案。"
  },
  "render_width": {
    "description": "默认渲染宽度",
    "type": "int",
    "default": 900,
    "hint": "菜单未设置手动宽度时的后备宽度；Page 默认会按内容、列数和卡片模板自动计算实际渲染宽度。"
  },
  "render_scale": {
    "description": "Typst 输出倍率",
    "type": "int",
    "default": 4,
    "hint": "仅用于旧菜单缺少 Page 完整预览 PNG 时的 Typst 兼容编译；已有完整预览图的菜单会直接复用 Page 像素结果。"
  },
  "show_render_error_detail": {
    "description": "聊天侧显示详细渲染错误",
    "type": "bool",
    "default": false,
    "hint": "建议仅调试时启用，避免向普通聊天暴露内部错误。"
  }
}
